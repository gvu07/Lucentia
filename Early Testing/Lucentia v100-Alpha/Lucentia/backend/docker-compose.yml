services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: lucentia
      POSTGRES_PASSWORD: lucentia
      POSTGRES_DB: lucentia
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data

volumes:
  dbdata:
